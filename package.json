{
  "name": "assurance-regent-direct-supabase",
  "version": "6.3.19",
  "private": true,
  "type": "module",
  "description": "Assurance Regent v6.3.19 with protected production JavaScript build, Supabase persistence, private storage hardening, integrity verification, functional role-based access control, and role-scoped dashboard analytics.",
  "scripts": {
    "build": "npm run apply:role-access && npm run apply:dashboard-access && npm run protect:client && npm run verify:protected && npm run verify:role-access && npm run verify:dashboard-access",
    "apply:role-access": "node scripts/apply-role-access.mjs",
    "apply:dashboard-access": "node scripts/apply-dashboard-access.mjs",
    "protect:client": "node scripts/protect-client.mjs",
    "verify:protected": "node scripts/verify-protected.mjs",
    "verify:role-access": "node scripts/verify-role-access.mjs",
    "verify:dashboard-access": "node scripts/verify-dashboard-access.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "javascript-obfuscator": "5.5.0"
  }
}
