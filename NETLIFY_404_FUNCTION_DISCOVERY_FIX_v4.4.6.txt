Assurance Regent v4.4.6 — Netlify 404 / Function Discovery Fix

Purpose
-------
Fixes the Netlify generic "Page not found" response at /api/health after the v4.4.5 api.js deployment.

Changes
-------
1. Reverts the function entry to api.cjs, the entry format Netlify previously discovered and invoked successfully for this project.
2. Loads the ESM server with dynamic import('../../server.js').
3. Explicitly includes server.js, src/**/*.js, data/**/*.json, and public/workbook-engine.js in the Netlify function artifact.
4. Uses the current Netlify [functions] directory = "netlify/functions" configuration syntax.
5. Adds public/_redirects as a second routing layer for /api/*.
6. Keeps /api/* -> /.netlify/functions/api/:splat in netlify.toml.

GitHub replacement
------------------
Upload/replace:
- netlify.toml
- package.json
- netlify/functions/api.cjs
- public/_redirects

Delete:
- netlify/functions/api.js
- netlify/functions/api.mjs

After GitHub commit and Netlify deployment, first test:
https://ar-intel.netlify.app/.netlify/functions/api/health
Then test:
https://ar-intel.netlify.app/api/health

Developer login remains:
Username: Dvp
Password: Abcd@1234
