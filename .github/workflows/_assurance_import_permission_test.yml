name: Assurance import permission test
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub workflow write permission confirmed"
