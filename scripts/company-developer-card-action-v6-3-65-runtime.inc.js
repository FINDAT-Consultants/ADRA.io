  /* Assurance Regent v6.3.65 — developer company card action-only refinement START */
  const renderDeveloperCompanySelectorBase65=renderDeveloperCompanySelector63;
  renderDeveloperCompanySelector63=function(){renderDeveloperCompanySelectorBase65();const host=$('developerCompanySelector63');if(!host)return;host.querySelectorAll('.developer-company-card63').forEach(card=>{const companyId=String(card.dataset.developerCompanyOpen63||'');card.removeAttribute('data-developer-company-open63');card.setAttribute('role','group');card.setAttribute('aria-label','Company information');const featureLine=card.querySelector('.developer-company-card-copy63 em');if(featureLine)featureLine.remove();let action=card.querySelector('.developer-company-open63');if(!action)return;const button=document.createElement('button');button.type='button';button.className='developer-company-open63';button.dataset.developerCompanyOpen63=companyId;button.innerHTML='Open Workspace <span aria-hidden="true">→</span>';action.replaceWith(button);});};
  /* Assurance Regent v6.3.65 — developer company card action-only refinement END */
