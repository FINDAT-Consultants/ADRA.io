import {createHash} from 'node:crypto';
import {existsSync,readFileSync,readdirSync,statSync} from 'node:fs';
import {basename,join,resolve} from 'node:path';
import vm from 'node:vm';

const root=process.cwd(),publicDir=resolve(root,'public'),currencyFile=resolve(root,'currencies.js'),netlifyFile=resolve(root,'netlify.toml'),holidayFunction=resolve(root,'supabase/functions/company-holidays/index.ts');
if(!existsSync(currencyFile))throw new Error('currencies.js is required for the registered-country flag picker.');
const sandbox={window:{}};vm.runInNewContext(readFileSync(currencyFile,'utf8'),sandbox,{filename:'currencies.js'});const rows=Array.isArray(sandbox.window.ADRA_CURRENCIES)?sandbox.window.ADRA_CURRENCIES:[],countries=new Map();for(const row of rows){const code=String(row?.countryCode||'').trim().toUpperCase(),name=String(row?.country||'').trim();if(/^[A-Z]{2}$/u.test(code)&&name&&!countries.has(code))countries.set(code,name);}if(countries.size<195)throw new Error(`Registered-country catalogue is incomplete: found ${countries.size}, expected at least 195.`);
for(const code of ['ZM','ZW','ZA','GB','US','IN','BR','JP','AU','CA'])if(!countries.has(code))throw new Error(`Registered-country flag catalogue is missing ${code}.`);
if(!existsSync(netlifyFile))throw new Error('netlify.toml is required.');const netlify=readFileSync(netlifyFile,'utf8');if(!netlify.includes("img-src 'self' data: blob: https://flagcdn.com"))throw new Error('Netlify CSP must allow https://flagcdn.com in img-src.');if(!netlify.includes('npm run build && node scripts/apply-developer-company-country-flags-v6-3-113.mjs'))throw new Error('Netlify must apply v6.3.113 after the standard build.');if(/npm run build[^\n]*protect-client\.mjs/u.test(netlify))throw new Error('Netlify v6.3.113 must not run the full client protector a second time.');
if(!existsSync(holidayFunction))throw new Error('Supabase company-holidays Edge Function source is missing.');const holidaySource=readFileSync(holidayFunction,'utf8');for(const token of ['registeredCountryCode','country_code','Nager.Date'])if(!holidaySource.includes(token))throw new Error(`Company holiday function is missing ${token}.`);
const targets=[resolve(root,'app.js')];if(existsSync(publicDir))for(const name of readdirSync(publicDir))if(/^app(?:\.|-).*\.js$/iu.test(name))targets.push(join(publicDir,name));for(const file of targets.filter(existsSync)){const source=readFileSync(file,'utf8');for(const token of ['COMPANY_PROFILE_COUNTRY_DROPDOWN_SCHEMA112','COMPANY_COUNTRY_FLAGS_SCHEMA113','countryFlagEnhanceSelect113','flagcdn.com/24x18/','Search country or code','assurance-regent-company-country-saved','saveCompanyProfileEditBefore113','saveCompanyMasterBefore113','persistedFields:[\'registeredCountry\',\'registeredCountryCode\']','calendarHolidayRefreshOnCountryChange:true','nativeSelectAuthoritative:true'])if(!source.includes(token))throw new Error(`Country flag/context verifier missing ${token} in ${basename(file)}.`);}
function walk(dir){return readdirSync(dir,{withFileTypes:true}).flatMap(entry=>{const file=join(dir,entry.name);return entry.isDirectory()?walk(file):[file];});}function sriFor(file){return `sha384-${createHash('sha384').update(readFileSync(file)).digest('base64')}`;}function versionFor(file){return createHash('sha256').update(readFileSync(file)).digest('hex').slice(0,16);}let sriBindings=0;if(existsSync(publicDir)){const htmlFiles=walk(publicDir).filter(file=>file.endsWith('.html'));for(const appFile of targets.filter(file=>file.startsWith(`${publicDir}/`)&&existsSync(file)&&statSync(file).isFile())){const name=basename(appFile),sri=sriFor(appFile),version=versionFor(appFile);for(const htmlFile of htmlFiles){const html=readFileSync(htmlFile,'utf8');if(!html.includes(`./${name}`))continue;if(!html.includes(`src="./${name}?v=${version}"`)||!html.includes(`integrity="${sri}"`))throw new Error(`Final SRI/version for ${name} is stale in ${basename(htmlFile)}.`);sriBindings+=1;}}}if(targets.some(file=>file.startsWith(`${publicDir}/`)&&existsSync(file))&&sriBindings<1)throw new Error('No final app-script SRI binding was verified.');
console.log(`[verify-developer-company-country-flags-v6-3-113] OK apps=${targets.filter(existsSync).length} countries=${countries.size} flag-images=1 saved-country-event=1 calendar-holidays=1 sri-bindings=${sriBindings}`);
