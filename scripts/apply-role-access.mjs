import { existsSync, readFileSync, readdirSync, writeFileSync } from 'node:fs';
import { basename, join, resolve } from 'node:path';

const root=process.cwd(),publicDir=resolve(root,'public'),targets=[];const rootApp=resolve(root,'app.js');if(existsSync(rootApp))targets.push(rootApp);if(existsSync(publicDir))for(const name of readdirSync(publicDir))if(/^app(?:\.|-).*\.js$/iu.test(name))targets.push(join(publicDir,name));if(!targets.length)throw new Error('No Assurance Regent application runtime was found for the RBAC patch.');
const legacyEmployeePolicies=["return {...base,role:'Employee',allowedViews:['dashboard','company','work','time','leave','employees','calendar']};}","return {...base,role:'Employee',allowedViews:['dashboard','work','time','leave','calendar']};}"];
const employeePolicy="return {...base,role:'Employee',allowedViews:['dashboard','company','work','time','leave','calendar']};}";
const financeOld="if(authority==='FINANCE_MANAGER')return {...base,allowedViews:['dashboard','insights','reports','work','time','projects','payroll','calendar','monthly','checks','exceptions','assurance','voucher','audit']";
const financeNew="if(authority==='FINANCE_MANAGER')return {...base,allowedViews:['dashboard','company','insights','reports','work','time','projects','payroll','calendar','monthly','checks','exceptions','assurance','voucher','audit']";
const auditorOld="if(authority==='AUDITOR')return {...base,allowedViews:['dashboard','reports','projects','calendar','monthly','checks','exceptions','assurance','voucher','audit']";
const auditorNew="if(authority==='AUDITOR')return {...base,allowedViews:['dashboard','company','reports','projects','calendar','monthly','checks','exceptions','assurance','voucher','audit']";
const oldSwitchStart="  function switchView(view){\n    if(view==='assistant'&&!developerStudioAllowed())return false;";
const guardedSwitchStart=`  function switchView(view){\n    /* v6.3.25 — enforce functional RBAC before rendering a protected view. */\n    const accessUser=controlUser(),accessPerms=controlPermissions(),accessAllowed=new Set(accessPerms?.allowedViews||[]),accessAll=accessAllowed.has('*');\n    if(accessUser&&view!=='dashboard'&&!accessAll&&!accessAllowed.has(view)){\n      const label=authorityLabel(functionalAuthority(effectiveUserOrg(accessUser)));\n      toast(\`Access restricted: \${label} does not have access to this section.\`);\n      view='dashboard';\n    }\n    if(view==='assistant'&&!developerStudioAllowed())return false;`;
const oldNavVisibility=`    document.querySelectorAll('.nav-item[data-view]').forEach(btn=>{btn.hidden=!u||(btn.dataset.view==='assistant'&&authority!=='DEVELOPER')||(!all&&!allowed.has(btn.dataset.view));});if(authority!=='DEVELOPER'&&state.view==='assistant')switchView('dashboard');\n    document.querySelectorAll('.nav-group').forEach(g=>{const visible=[...g.querySelectorAll('.nav-item')].some(x=>!x.hidden);g.hidden=!visible;});`;
const hardenedNavVisibility=`    /* v6.3.26 — unauthorized sidebar features must not be rendered, focused, or exposed to assistive navigation. */\n    const setSidebarItemVisibility=(el,visible)=>{if(!el)return;if(visible){el.hidden=false;el.style.removeProperty('display');el.removeAttribute('aria-hidden');el.removeAttribute('tabindex');}else{el.hidden=true;el.style.setProperty('display','none','important');el.setAttribute('aria-hidden','true');el.setAttribute('tabindex','-1');}};\n    document.querySelectorAll('.nav-item[data-view]').forEach(btn=>{const permitted=!!u&&!(btn.dataset.view==='assistant'&&authority!=='DEVELOPER')&&(all||allowed.has(btn.dataset.view));setSidebarItemVisibility(btn,permitted);});if(authority!=='DEVELOPER'&&state.view==='assistant')switchView('dashboard');\n    document.querySelectorAll('.nav-group').forEach(g=>{const visible=[...g.querySelectorAll('.nav-item')].some(x=>!x.hidden&&x.style.display!=='none');g.hidden=!visible;if(visible){g.style.removeProperty('display');g.removeAttribute('aria-hidden');}else{g.style.setProperty('display','none','important');g.setAttribute('aria-hidden','true');}});`;
let changed=0;for(const file of targets){let source=readFileSync(file,'utf8'),original=source;for(const old of legacyEmployeePolicies)if(source.includes(old)&&old!==employeePolicy)source=source.replace(old,employeePolicy);if(source.includes(financeOld))source=source.replace(financeOld,financeNew);if(source.includes(auditorOld))source=source.replace(auditorOld,auditorNew);if(!source.includes(employeePolicy)||!source.includes(financeNew)||!source.includes(auditorNew))throw new Error(`All-user Company directory policy was not established in ${basename(file)}.`);if(source.includes(oldSwitchStart))source=source.replace(oldSwitchStart,guardedSwitchStart);else if(!source.includes('v6.3.25 — enforce functional RBAC before rendering a protected view.'))throw new Error(`switchView RBAC insertion point was not found in ${basename(file)}.`);if(source.includes(oldNavVisibility))source=source.replace(oldNavVisibility,hardenedNavVisibility);else if(!source.includes('v6.3.26 — unauthorized sidebar features must not be rendered, focused, or exposed to assistive navigation.'))throw new Error(`Sidebar visibility RBAC insertion point was not found in ${basename(file)}.`);if(source!==original){writeFileSync(file,source,'utf8');changed++;}console.log(`[rbac] ${basename(file)} company-directory=all-signed-in-users direct-view-guard=enabled sidebar=fully-hidden-when-unauthorized`);}console.log(`[rbac] functional sidebar policy verified across ${targets.length} runtime file(s); ${changed} file(s) updated.`);
