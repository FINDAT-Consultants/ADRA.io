import {existsSync,readFileSync,readdirSync,writeFileSync} from 'node:fs';
import {basename,join,resolve} from 'node:path';
const root=process.cwd(),publicDir=resolve(root,'public'),targets=[resolve(root,'app.js')];if(existsSync(publicDir))for(const n of readdirSync(publicDir))if(/^app(?:\.|-).*\.js$/iu.test(n))targets.push(join(publicDir,n));
let n=0;for(const file of targets.filter(existsSync)){let s=readFileSync(file,'utf8'),before=s;s=s.replaceAll("if(aiNav){aiNav.hidden=!can;aiNav.dataset.permissionHidden=can?'false':'true';}","if(aiNav){aiNav.hidden=false;aiNav.dataset.permissionHidden='false';}");if(!s.includes("aiNav.hidden=false;aiNav.dataset.permissionHidden='false'"))throw new Error(`AI Settings all-user visibility patch failed in ${basename(file)}`);if(s!==before){writeFileSync(file,s,'utf8');n++;}console.log(`[ai-preferences-access] ${basename(file)} personal-ai-settings=all-signed-in-users`);}console.log(`[ai-preferences-access] ${n} runtime file(s) updated.`);
