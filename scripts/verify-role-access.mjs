import { existsSync, readFileSync, readdirSync } from 'node:fs';
import { join, resolve } from 'node:path';
const publicDir=resolve(process.cwd(),'public');if(!existsSync(publicDir))throw new Error('public/ directory is missing.');const appName=readdirSync(publicDir).find(name=>/^app(?:\.|-).*\.js$/iu.test(name));if(!appName)throw new Error('Published Assurance Regent app runtime is missing.');const source=readFileSync(join(publicDir,appName),'utf8');
const required=[
 ["if(role==='Developer')return 'DEVELOPER'",'Developer authority detector'],["return 'HR_MANAGER'",'HR Manager authority detector'],["return 'FINANCE_MANAGER'",'Finance Manager authority detector'],["return 'PROGRAMS_MANAGER'",'Programs Manager/Director authority detector'],["return 'PROJECT_MANAGER'",'Project Manager authority detector'],["return 'HEAD_OF_DEPARTMENT'",'Head of Department authority detector'],["return 'SUPERVISOR'",'Supervisor authority detector'],["if(role==='Administrator')return 'ADMINISTRATOR'",'Administrator fallback authority'],
 ["allowedViews:['dashboard','company','work','time','leave','calendar']",'ordinary employee personal-work plus Company-directory policy'],["authority==='HR_MANAGER'",'HR permission profile'],["'recruiting','onboarding'",'HR recruitment/onboarding access'],["if(authority==='FINANCE_MANAGER')return {...base,allowedViews:['dashboard','company'",'Finance Company directory access'],["if(authority==='AUDITOR')return {...base,allowedViews:['dashboard','company'",'Auditor Company directory access'],["'payroll','calendar','monthly','checks','exceptions','assurance','voucher','audit'",'Finance controls'],["authority==='PROGRAMS_MANAGER'",'Programs permission profile'],["authority==='PROJECT_MANAGER'",'Project permission profile'],['v6.3.25 — enforce functional RBAC before rendering a protected view.','direct-view RBAC guard'],['v6.3.26 — unauthorized sidebar features must not be rendered, focused, or exposed to assistive navigation.','strict sidebar visibility guard'],["document.querySelectorAll('.nav-item[data-view]')",'sidebar visibility enforcement'],["style.setProperty('display','none','important')",'CSS-resistant sidebar hiding'],["setAttribute('aria-hidden','true')",'assistive-navigation hiding'],["setAttribute('tabindex','-1')",'keyboard-focus removal'],["document.querySelectorAll('.nav-group')",'empty sidebar group hiding']];
for(const [needle,label] of required)if(!source.includes(needle))throw new Error(`RBAC verification failed: ${label} is missing.`);
if(source.includes("role:'Employee',allowedViews:['dashboard','company','work','time','leave','employees','calendar']"))throw new Error('RBAC verification failed: ordinary employees still expose Employees management.');
console.log('[rbac-verify] OK: functional authority detection is present.');
console.log('[rbac-verify] OK: Company directory is available to all signed-in functional authorities while ordinary Employee remains personal-work scoped outside Company.');
console.log('[rbac-verify] OK: ordinary Employee does not expose Employees management.');
console.log('[rbac-verify] OK: HR, Finance, Programs, Projects, Department Head, Supervisor and Auditor profiles remain function-scoped.');
console.log('[rbac-verify] OK: unauthorized direct view navigation is guarded before rendering.');
console.log('[rbac-verify] OK: unauthorized sidebar items are hidden, removed from focus/assistive navigation, and empty groups are hidden.');
